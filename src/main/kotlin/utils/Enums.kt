package utils

enum class PetState() {
    IDLE,
    SLEEPING,
    MOVING
    //EATING
}

enum class Options() {
    PET,
    TALK,
    FEED,
    PLAY,
    SIT
}

enum class Orientation {
    LEFT,
    RIGHT
}

enum class Emotion {
    CATFACE,
    LOVED,
    SLEEPY,
    HUNGRY
}